# Plausible Analytics Configuration
# Copy this file to .env and fill in your actual values

# Required: Your Plausible Analytics API key
PLAUSIBLE_API_KEY=your_api_key_here

# Optional: Default site ID (can be overridden per query)
PLAUSIBLE_SITE_ID=your_site_id_here

# Optional: Plausible API URL (defaults to https://plausible.io/api/v2/query)
# Uncomment and modify if using self-hosted Plausible
# PLAUSIBLE_API_URL=https://your-plausible-instance.com/api/v2/query
